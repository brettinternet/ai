---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: '3'

tasks:
  check:
    desc: Check project
    cmds:
      - ruff check

  fix:
    desc: Format project
    cmds:
      - ruff format
